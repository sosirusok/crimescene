name: Customer deployment maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Customer deployment is being upgraded."
